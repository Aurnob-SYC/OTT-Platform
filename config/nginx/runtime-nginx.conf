worker_processes  1;

events {
    worker_connections  1024;
}

http {
    include       D:/tools/nginx/nginx-1.30.2/conf/mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout  65;

    include D:/OTT-Platform/config/nginx/chapter-1-hls.conf;
}
